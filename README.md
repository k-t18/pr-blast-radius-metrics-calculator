# PR Blast Radius Metrics Calculator

Analyze the impact and blast radius of pull requests — measuring how many files, modules, and downstream dependencies a PR touches.

## Features

- Calculate file-level and module-level blast radius for any PR
- Identify downstream dependencies affected by changes
- Generate risk scores based on change surface area
- Support for GitHub PRs via the GitHub API
